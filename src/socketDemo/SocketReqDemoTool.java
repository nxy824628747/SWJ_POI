package socketDemo;import java.io.*;import java.net.Socket;public class SocketReqDemoTool {    static String host="127.0.0.1";    static String port="8382";    static String path="";    public static void main(String[] args) {        System.out.println("开启客户端");        try {            Socket socket = new Socket("127.0.0.1", 8382);            BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in, "UTF-8"));            BufferedReader bufferedReaderSocket = new BufferedReader(new InputStreamReader(socket.getInputStream(), "UTF-8"));            BufferedWriter bufferedWriter = new BufferedWriter(new OutputStreamWriter(socket.getOutputStream(), "UTF-8"));            //收数据            new Thread(new Runnable() {                @Override                public void run() {                    try {                        while (true) {                            String str;                            while ((str = bufferedReaderSocket.readLine()) != null) {                                System.out.println("服务端回复  ：" + str);                            }                        }                    } catch (IOException e) {                        e.printStackTrace();                    }                }            }).start();            //发数据            while (true) {                String str = bufferedReader.readLine();                bufferedWriter.write(str);                bufferedWriter.write("\n");                bufferedWriter.flush();                System.out.println("已发送:  " + str);            }        } catch (IOException e) {            e.printStackTrace();        }    }}